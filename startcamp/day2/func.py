# 함수는 return을 적어주지 않으면 None을 return한다.

def funcname(parameter_list):
    """
    docstring
    """
    pass

def mul(a, b):
    result = a + b
    return result

print(mul(1,2))
# print(mul('가','나'))
# print(mul([3,4],[1,2]))

# help 함수를 이용하면 사용법을 볼 수 있다.
# print(help(len))

# 유닉스 계열의 운영체제는 Bash를 제공 (셸)
# 맥은 기본 셸이 Bash다.
# 윈도우에서는 명령 프롬프트를 제공
# 윈도우에서 Bash를 쓰기 위해서 Git Bash를 쓰면 된다.

# 리눅스는 유닉스에서 파생함
# 운영 체제 크게 3가지 (리눅스 유닉스 윈도우즈)